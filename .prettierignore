.DS_Store
node_modules
/build
/.svelte-kit
/package

# Generated by `npm run package`. Linting build output produces noise rather
# than findings, and without this the result of `npm run lint` depends on whether
# you have built recently. Matches the `dist/**` ignore in eslint.config.js.
/dist
.env
.env.*
!.env.example

# One row per spec rule id in a format a reviewer-side checker parses. Prettier pads
# every cell to its column's widest, so a single evidence edit rewrote all 79 rows and
# the canonical header lines stopped reading as written. Checked instead by
# `node _dev_/conformance-summary.mjs --check`.
CONFORMANCE.md

# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock
